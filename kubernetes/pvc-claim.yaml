apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: manual-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: aws-ebs-sc  # Reference the AWS EBS storage class
  resources:
    requests:
      storage: 100Mi