apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ebs-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: aws-ebs-sc  # Reference the AWS EBS storage class
  resources:
    requests:
      storage: 10Gi